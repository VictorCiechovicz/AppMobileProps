import { StyleSheet } from 'react-native'

const estilos = StyleSheet.create({
  boxSignos: {
    padding: 10,
    borderWidth: 1,
    borderColor: '#000',
    borderRadius: 2,
    marginBottom: 10
  },
  nomeSignos: {
    fontSize: 18,
    marginBottom: 5
  }
})

export default estilos
